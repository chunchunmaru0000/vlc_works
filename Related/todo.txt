{
+   4. "Е" кнопка- придумать какой то другой сигнал для списывания оплаченных денег. 
+   Выявился такой баг. Если игрок заплатил деньги и не стал играть и просто ушел. 
+   То аппарат не списывает эти деньги . Не защитывает . Такое было один раз .

    Теперь должно считать заплаченное сразу после того как заплатили, а не в конце вместе
    с добавлением записей в бд и эксель
}, {
+-   2. Сделать чтобы при ручном вводе цифр в окне номер игрока, 
+-   цифры не появлялись на экране в аппарате. Или как то по другому.... 
+   5. Сделать кнопку " убрать цифры " с экрана в ручную.
}, {
    [пока не ясно как]
-   1. Сделать что бы тестировалось подъем и опускание камеры ФК. 
-   Перед загрузкой программы. И сделать например надпись" ок. Система готова. " 
}, {
+   3. Сделать возможность оперативного отключения датчиков присутствия, 
    [пока не ясно как]
-   и ручного включения игры. 
},


[потом]
{
    То есть вот человек нагнулся и только пол головы
    А потом выпрямился и его узнали
    Возможно дополнительный опрос блока ФК  поможет
}, 
[баг или фича?] 
{
    2) связано с нехваткой видео и на самом по итогу когда 
    будут все видео игр то этого бага и не будет но сейчас, 
    есть видео 0-4С и 0-9К, когда игрок доходит до С4 и побеждает 
    то он все также переходит на К4, после все также на К5 но 
    при этом уровеьнь С5 он иметь не будет за победу в С4, так как 
    нет видео С5 в файле скрипта, кроме этого при распознавании игрока
    а точнее при взятии его из бд, если у него например С4(максимум) 
    и К4 или больше(5,6,7,8,9), то будет он играть С4, а его уровень К 
    сбросится до 4, так как играет он С4, а после С4 идет К4
},
[баг]
{
    на форме бд, если выбрать фото 
    а потом обновить таблицу то
    фото больше не будет помечено как не сохраненное
},
{
Подробная информация об использовании оперативной 
(JIT) отладки вместо данного диалогового 
окна содержится в конце этого сообщения.

************** Текст исключения **************
System.InvalidOperationException: В данный момент объект используется другим процессом.
   в System.Drawing.Image.get_Width()
   в System.Drawing.Image.get_Size()
   в System.Windows.Forms.PictureBox.ImageRectangleFromSizeMode(PictureBoxSizeMode mode)
   в System.Windows.Forms.PictureBox.OnPaint(PaintEventArgs pe)
   в System.Windows.Forms.Control.PaintWithErrorHandling(PaintEventArgs e, Int16 layer)
   в System.Windows.Forms.Control.WmPaint(Message& m)
   в System.Windows.Forms.Control.WndProc(Message& m)
   в System.Windows.Forms.Control.ControlNativeWindow.OnMessage(Message& m)
   в System.Windows.Forms.Control.ControlNativeWindow.WndProc(Message& m)
   в System.Windows.Forms.NativeWindow.Callback(IntPtr hWnd, Int32 msg, IntPtr wparam, IntPtr lparam)
   при когда чето там в EditDbForm
}
{
    Теперь должно считать заплаченное сразу после того как заплатили, а не в конце вместе
    с добавлением записей в бд и эксель, таким образом при этом игра засчитана может и не быть
    и может быть расхождение с числами на сервере, что значит что либо запрос о том что заплатили
    сразу должен идти на сервер или или.
}