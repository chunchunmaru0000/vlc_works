{
+   будет также один файл, но с него с какого-то момента будет 
+       метка средних 
+       и потом сложных
+
+   Три таблицы будут изначально одинаковы 
+       только в легком будут все уровни, 
+       в среднем например уроани все после метки средние, 
+       в сложном после метки сложные
+
+   если игрок выигрывает подряд 3 игры, 
+       его переключают на следующую сложность
+
+   если игрок проигрывает 3 игры подряд. 
+       Дать ему более лёгкий уровень игры Опустить на строчку в таблице
+
+       если опускается ниже последней строки в сложном например 
+       то переходит на строку ниже в сначала в средний
},
{
    из бд брать WonCounter
+   фикс -1 индекса при записи игрока
},



[потом]
{
    сделать из бд конвертер в excel может быть
    а может сразу в гугл таблицы делать изменения
    
    гугл таблицы а там вроде и не так сложно
    https://developers.google.com/sheets/api/guides/concepts?hl=ru
},